import React from 'react';
import { Container, Typography } from '@mui/material';

export default function AppointmentsPage() {
  return (
    <Container sx={{ mt: 4 }}>
      <Typography variant="h5">Appointments Page (MVP)</Typography>
    </Container>
  );
}
