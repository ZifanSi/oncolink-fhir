import React from 'react';
import { Container, Typography } from '@mui/material';

export default function TreatmentPage() {
  return (
    <Container sx={{ mt: 4 }}>
      <Typography variant="h5">Treatment Tracking Page (MVP)</Typography>
    </Container>
  );
}
