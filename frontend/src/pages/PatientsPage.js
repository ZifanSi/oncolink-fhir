import React from 'react';
import { Container, Typography } from '@mui/material';

export default function PatientsPage() {
  return (
    <Container sx={{ mt: 4 }}>
      <Typography variant="h5">Patients Page (MVP)</Typography>
    </Container>
  );
}
