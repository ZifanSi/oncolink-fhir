import React from 'react';
import { Container, Typography } from '@mui/material';

export default function RecordsPage() {
  return (
    <Container sx={{ mt: 4 }}>
      <Typography variant="h5">Medical Records Page (MVP)</Typography>
    </Container>
  );
}
