import React from 'react';
import { Container, Typography } from '@mui/material';

export default function MenuPage() {
  return (
    <Container sx={{ mt: 4 }}>
      <Typography variant="h5">this is menu page only</Typography>
    </Container>
  );
}
